# 注意力机制

注意力机制的直觉是：模型在理解一个 token 时，不应该平均看待所有 token，而是应该给更相关的 token 更高权重。

Self-attention 会根据 query、key、value 计算不同 token 之间的关系。query 可以理解成“我想找什么”，key 可以理解成“我有什么特征”，value 可以理解成“真正要传递的信息”。

Multi-head attention 会同时从多个角度观察文本关系。有的头可能关注主谓关系，有的头可能关注指代关系，有的头可能关注句子结构。

注意力机制不是人工写死的规则，而是模型通过训练数据学出来的参数。
